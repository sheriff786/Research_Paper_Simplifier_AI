# Enhanced Stage 2: Multimodal Document Parsing - Requirements

# Core Dependencies
jupyter==1.1.1
notebook==7.3.2
ipykernel==6.29.5
python-dotenv==1.0.0

# PDF Processing - TEXT
PyMuPDF==1.24.0
pdfplumber==0.11.0

# PDF Processing - TABLES (Choose one or both)
camelot-py[cv]==0.11.0  # Best for tables, requires OpenCV
# tabula-py==2.9.0  # Alternative if Camelot doesn't work

# PDF Processing - IMAGES
pdf2image==1.17.0  # Convert PDF pages to images
Pillow==10.4.0  # Image processing

# AI & LLM
crewai==0.86.0
crewai-tools==0.15.0
langchain==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0
openai==1.54.0

# Vector Database & Embeddings
faiss-cpu==1.9.0  # Use faiss-gpu==1.9.0 if you have CUDA
sentence-transformers==3.3.1
tiktoken==0.8.0

# Local Embeddings (Optional - Free alternative to OpenAI)
transformers==4.46.3
torch==2.5.1

# Data Processing
numpy==2.1.3
pandas==2.2.3

# Visualization
matplotlib==3.9.0
seaborn==0.13.2

# Additional for table extraction
openpyxl==3.1.5  # Excel export
tabulate==0.9.0  # Table formatting

# System dependencies for Camelot (install via system package manager)
# Ubuntu/Debian: sudo apt-get install python3-tk ghostscript
# macOS: brew install ghostscript tcl-tk
# Windows: Download Ghostscript from https://ghostscript.com/
